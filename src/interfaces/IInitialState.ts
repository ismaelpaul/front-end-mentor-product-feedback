export interface InitialStateProdRequests {
	productRequests: [];
	isError: boolean;
	isSuccess: boolean;
	isLoading: boolean;
	message: unknown;
}

export interface InitialStateSidebar {
	isSidebarOpen: boolean;
}
